/*
 __ ++valor -> fará a ação ser com o valor + 1.
 __valor-- ->  aciona a ação antes de adicionar + 1 a valor.


 PERGUNTAR AO Tesch sobre o endereço em decimal no Ponteiros e +Ponteiros. (Colocar pra rodar)

__ sizeof(variável):
- Usado para ver o número de bytes que uma variavel tem.

- chama com: %ld
- identifica com: printf("...",sizeof(var));

ex: printf("O valor %d tem %ld bytes\n", valores[i], sizeof(valores[i]));


__(...,strtok(var, "\n"));

- usado para tirar os \n que tem dentro da ação.






















*/